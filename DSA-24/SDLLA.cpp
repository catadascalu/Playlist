/*
#include "SDLLA.h"

SDLLA & SDLLA::operator=(SDLLA& sdlla1)
{
	for (int i = 0; i < sdlla1.cap; i++)
		this->nodes[i] = sdlla1.nodes[i];
	this->cap = sdlla1.getCap();
	this->head = sdlla1.getHead();
	this->tail = sdlla1.getTail();
	this->firstEmpty = sdlla1.getFE();


	return *this;
}
*/